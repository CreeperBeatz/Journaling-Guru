// App is the protected route layout. Real layout/chrome lives in AppShell;
// keep this file as the import surface so router.tsx stays stable.
export { AppShell as App } from "@/components/shell/AppShell";
